[project]
name = "usermint-api"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic>=2",
  "redis",
  "requests",
  "google-cloud-storage",
]

[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
solc = '0.8.24'
